# vi: set ft=ruby :

metadata

cookbook 'up2date', :git => 'git://github.com/TheSerapher/chef-up2date.git'
cookbook 'mysql', :git => 'git://github.com/TheSerapher/mysql.git', :ref => 'COOK-3773'

group :integration do
    cookbook 'minitest-handler'
end
